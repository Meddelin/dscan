import styled from 'styled-components';
import { Button } from '@beaver-ui/button';

// `styled(Beaver)` — purist shadow (§3.6).
export const StyledButton = styled(Button)`
  background: firebrick;
  color: white;
`;
